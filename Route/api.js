

const Express=(request,resolve)=>{
   Response.write(
    resolve.write(([
        {node: "What is Express"},
        {Defination : "Express allows developers to write JavaScript code that runs directly in a computer process itself instead of in a browser"}  
        
    ]))
)}


module.exports = {Express}